## Initializing a typescript project

 ```js
npm init -y
npx tsc --init
```

- Config tsconfig.json
    - Set `rootDir` = `./src`
    - Set `outDir` = `./dist`

- Complile `Typescript`
    ```ts
    tsc -b or npx tsc -b
    ```